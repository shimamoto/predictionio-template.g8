# predictionio-template.g8

** This template is currently experimental. **

Giter8 template for generating a new Engine from the PredictionIO official templates.

## Running

First, create a project:

```sh
$ sbt new shimamoto/predictionio-template.g8
```

Then go to your project directory, and type `g8Scaffold <template>` from sbt to create an engine template:

```sh
$ cd myengine
$ sbt
> g8Scaffold recommender
```
